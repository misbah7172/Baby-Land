{
  "name": "baby-land",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "build": "npm run build -w backend && npm run build -w frontend",
    "test": "npm run test -w backend",
    "dev": "echo Use docker-compose up for the full stack"
  }
}